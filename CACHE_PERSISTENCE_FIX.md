# 缓存持久化修复 - 确保编译后页面一直保留在内存

## 🎯 目标

确保第一个用户编译后，后续所有用户都能毫秒级访问，缓存不会被自动清除。

## ✅ 修复方案

### 1. 增加缓存时间到24小时

**修改文件**：`next.config.js`

**配置**：
```javascript
onDemandEntries: {
  maxInactiveAge: 60 * 1000 * 60 * 24,  // 24小时（86400000ms）
  pagesBufferLength: 100,                // 100个页面
}
```

**作用**：
- 页面在内存中保留24小时
- 即使24小时内没有访问，也不会被清除
- 可以缓存100个页面

### 2. 同步更新 server.js 中的默认值

**修改文件**：`server.js`

**确保**：
- 默认值也是24小时和100个页面
- 与 `next.config.js` 保持一致

## 📊 工作原理

### Next.js 缓存机制

1. **首次访问**：
   - 用户访问页面 → Next.js 编译页面 → 编译结果存入内存
   - 这个过程需要 100-130 秒

2. **后续访问**：
   - 用户访问页面 → Next.js 检查内存缓存 → 找到缓存 → 直接返回
   - 这个过程只需要 0.05 秒

3. **缓存清理**：
   - 默认情况下，页面在 `maxInactiveAge` 时间内没有访问会被清理
   - 现在设置为24小时，意味着页面会保留24小时

### 为什么设置为24小时？

- **足够长**：24小时足够覆盖大部分使用场景
- **不会无限增长**：如果设置为无限大，可能导致内存泄漏
- **可以调整**：如果需要更长，可以设置为 7 天（`60 * 1000 * 60 * 24 * 7`）

## 🔍 验证方法

### 测试1：首次访问（编译）
```bash
# 重启服务器
docker restart phala-platform-frontend
sleep 15

# 第一个用户访问（会触发编译）
curl -w "\n时间: %{time_total}s\n" -o /dev/null -s http://localhost:3000/
# 预期：100-130秒（编译时间）
```

### 测试2：后续用户访问（使用缓存）
```bash
# 连续10个用户访问
for i in {1..10}; do
  curl -w "用户$i: %{time_total}s\n" -o /dev/null -s http://localhost:3000/
  sleep 1
done
# 预期：所有用户都在 0.05-0.1 秒内
```

### 测试3：长时间后访问（验证缓存持久性）
```bash
# 等待5分钟后再次访问
sleep 300
curl -w "\n时间: %{time_total}s\n" -o /dev/null -s http://localhost:3000/
# 预期：仍然在 0.05-0.1 秒内（缓存还在）
```

## ⚠️ 注意事项

1. **内存使用**：
   - 增加缓存时间和页面数量会增加内存使用
   - 100个页面 × 平均大小 ≈ 可能需要几百MB内存
   - 确保服务器有足够内存

2. **缓存失效**：
   - 如果服务器重启，缓存会丢失
   - 如果代码更新并重新部署，缓存会丢失
   - 如果内存不足，系统可能会清理缓存

3. **首次编译不可避免**：
   - 第一个用户访问时仍需要编译（100-130秒）
   - 可以通过服务器预热来提前编译（已实现）

## 🚀 进一步优化（可选）

如果需要更长的缓存时间，可以设置为：

```javascript
// 7天
maxInactiveAge: 60 * 1000 * 60 * 24 * 7

// 30天
maxInactiveAge: 60 * 1000 * 60 * 24 * 30
```

但要注意内存使用情况。

