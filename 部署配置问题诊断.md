# 部署配置问题诊断报告

## 🔴 发现的问题

### 1. **Docker容器没有资源限制** ⚠️
- 容器没有设置CPU和内存限制
- 虽然服务器资源充足，但可能导致资源分配不均

### 2. **首页HTML文件过大（2.5MB）** 🔴 严重
- **这是主要问题！**
- Ant Design的CSS全部内联到HTML中
- 导致每次访问首页都需要下载2.5MB数据
- 在低带宽环境下会非常慢

### 3. **没有启用Node.js性能优化** ⚠️
- 没有设置`NODE_OPTIONS`优化参数
- 没有启用V8优化选项
- 没有设置合适的堆内存大小

### 4. **Nginx配置可以优化** ⚠️
- 没有启用HTTP/2
- 没有启用连接池优化
- 没有启用响应缓存

### 5. **可疑进程持续出现** 🔴 安全风险
- 发现反向shell进程尝试连接外部服务器
- 可能是依赖包被注入恶意代码
- 需要立即处理

---

## 📊 当前状态

| 指标 | 当前值 | 正常值 | 状态 |
|------|--------|--------|------|
| CPU使用率 | 0% | 10-30% | ⚠️ 过低（可能有问题） |
| 内存使用 | 191MB | 200-500MB | ✅ 正常 |
| 首页HTML大小 | 2.5MB | < 100KB | 🔴 严重超标 |
| Docker资源限制 | 无 | 建议设置 | ⚠️ 未设置 |

---

## 🎯 根本原因分析

### 为什么CPU和内存使用率低，但服务慢？

1. **首页HTML太大（2.5MB）**
   - 问题不在CPU/内存，而在**网络传输**
   - 即使服务器处理快，传输2.5MB也需要时间
   - 在低带宽环境下会更慢

2. **没有启用压缩**
   - 虽然Nginx配置了gzip，但可能没有生效
   - 需要检查gzip是否正常工作

3. **没有启用HTTP/2**
   - HTTP/1.1限制了并发连接
   - 多个资源需要串行加载

4. **Ant Design CSS内联**
   - 所有CSS都在HTML中，无法利用浏览器缓存
   - 每次访问都需要下载完整CSS

---

## ✅ 优化方案

### 方案1：优化首页HTML大小（最重要）

**问题：** 首页HTML 2.5MB，主要是Ant Design CSS内联

**解决：**
1. 使用Ant Design的按需加载
2. 延迟加载非关键组件
3. 优化CSS提取策略

### 方案2：优化Docker部署配置

**添加资源限制和优化：**
```bash
docker run -d --name phala-platform-frontend \
  --network host \
  --memory="2g" \
  --cpus="2" \
  -e NODE_OPTIONS="--max-old-space-size=1536 --dns-result-order=ipv4first" \
  -v /root/tmp/phala-blockchain-setup:/app/phala-blockchain-setup:ro \
  phala-platform-frontend
```

### 方案3：优化Nginx配置

**添加：**
- HTTP/2支持
- 响应缓存
- 连接池优化

### 方案4：启用Node.js性能优化

**添加环境变量：**
```bash
NODE_OPTIONS="--max-old-space-size=1536 --dns-result-order=ipv4first --enable-source-maps=false"
```

---

## 🔧 立即可以做的优化

1. **优化首页HTML大小**（最重要）
2. **添加Docker资源限制**
3. **启用Nginx HTTP/2**
4. **优化Node.js启动参数**

---

## ⚠️ 安全警告

发现可疑进程持续出现，建议：
1. 检查`package.json`依赖
2. 检查构建脚本
3. 考虑重新安装依赖
4. 检查是否有恶意依赖包

