# 服务器性能问题分析报告

## 问题概述
虽然服务器CPU和内存占用不高，但前端请求接口和页面加载都很慢。经过代码分析，发现了以下主要性能瓶颈：

---

## 🔴 主要性能问题

### 1. **Polkadot API连接管理问题**（严重）

**问题描述：**
- 多个API路由文件各自创建独立的Polkadot API连接
- 每个API路由都有自己的`getApi()`函数，没有全局连接池
- 连接创建和初始化需要30秒超时，如果连接失败会阻塞请求

**影响文件：**
- `/src/app/api/scheduling/route.ts` - 每次请求都可能创建新连接
- `/src/app/api/worker-data/route.ts` - 独立的API连接
- `/src/lib/phalaApi.ts` - 虽然有连接复用，但检查逻辑可能不够健壮

**性能影响：**
- 首次连接需要30秒
- 如果连接断开，重新连接会阻塞所有请求
- 多个API路由同时请求时，可能创建多个连接，浪费资源

---

### 2. **Worker检查超时累积问题**（严重）

**问题描述：**
- `/api/scheduling/workers/route.ts`中每个worker检查有10秒超时
- 虽然使用`Promise.all`并行检查，但如果某个worker响应慢，整个请求会等待
- 检查多个worker时，最慢的worker决定了整体响应时间

**代码位置：**
```typescript
// /src/app/api/scheduling/workers/route.ts
const response = await fetch(infoUrl, {
    cache: "no-store",
    signal: AbortSignal.timeout(10_000), // 10秒超时
});
```

**性能影响：**
- 如果有3个worker，最慢的worker需要8秒响应，整个API请求就需要8秒
- 如果某个worker完全无响应，需要等待10秒超时

---

### 3. **前端并发请求过多**（中等）

**问题描述：**
- 页面加载时会同时发起多个API请求
- 多个页面有自动刷新轮询，间隔从60秒到120秒不等
- 没有请求去重和合并机制

**发现的轮询：**
- `/management/scheduling/page.tsx` - 120秒自动刷新
- `/polkadot-wall/page.tsx` - 100秒自动刷新
- `/management/key-rotation/page.tsx` - 60秒自动刷新

**性能影响：**
- 页面首次加载时，多个API请求同时发起，服务器压力大
- 自动刷新时，多个页面同时请求，可能导致服务器响应变慢

---

### 4. **Nginx配置优化不足**（中等）

**问题描述：**
- 超时时间设置很长（300秒），但没有连接池优化
- 没有对API响应进行缓存
- 没有启用HTTP/2
- 没有启用gzip压缩优化（虽然有配置，但可能不够完善）

**当前配置：**
```nginx
proxy_connect_timeout 300s;
proxy_send_timeout 300s;
proxy_read_timeout 300s;
```

**性能影响：**
- 没有连接复用，每次请求都需要建立新连接
- 没有缓存，重复请求无法利用缓存
- HTTP/1.1限制了并发连接数

---

### 5. **Next.js API路由缺少缓存**（中等）

**问题描述：**
- API路由没有使用Next.js的缓存机制
- 所有API请求都标记为`cache: "no-store"`
- 链上查询结果没有缓存，每次请求都重新查询

**代码示例：**
```typescript
const response = await fetch(infoUrl, {
    cache: "no-store", // 禁用缓存
});
```

**性能影响：**
- 相同数据的重复查询浪费资源
- 链上查询本身就很慢，没有缓存会重复执行

---

### 6. **链上查询阻塞**（中等）

**问题描述：**
- 多个API路由都进行链上查询（Polkadot API查询）
- 链上查询是同步阻塞的，需要等待区块链节点响应
- 没有使用后台任务或队列处理

**影响文件：**
- `/src/app/api/scheduling/route.ts` - 查询workers信息
- `/src/lib/phalaApi.ts` - 多个链上查询函数

**性能影响：**
- 链上查询可能需要几秒到几十秒
- 如果区块链节点响应慢，所有请求都会被阻塞

---

## 📊 性能瓶颈总结

| 问题 | 严重程度 | 影响范围 | 预计优化效果 |
|------|---------|---------|------------|
| Polkadot API连接管理 | 🔴 严重 | 所有API路由 | 减少30秒连接时间 |
| Worker检查超时 | 🔴 严重 | `/api/scheduling/workers` | 减少5-10秒响应时间 |
| 前端并发请求 | 🟡 中等 | 所有页面 | 减少50%请求数 |
| Nginx配置 | 🟡 中等 | 所有请求 | 提升20-30%响应速度 |
| API缓存缺失 | 🟡 中等 | 所有API路由 | 减少重复查询 |
| 链上查询阻塞 | 🟡 中等 | 涉及链上查询的API | 减少阻塞时间 |

---

## 🎯 优化建议优先级

### 高优先级（立即优化）
1. **统一Polkadot API连接管理** - 创建全局连接池
2. **优化Worker检查超时** - 减少超时时间，使用更智能的超时策略
3. **添加API响应缓存** - 对可缓存的数据启用缓存

### 中优先级（近期优化）
4. **优化Nginx配置** - 启用HTTP/2，优化连接池
5. **减少前端并发请求** - 合并请求，添加请求去重
6. **优化轮询频率** - 根据数据更新频率调整轮询间隔

### 低优先级（长期优化）
7. **使用后台任务处理链上查询** - 将长时间查询移到后台
8. **启用Next.js ISR** - 对静态数据使用增量静态再生

---

## 📝 下一步行动

1. 先优化Polkadot API连接管理（影响最大）
2. 优化Worker检查逻辑（减少超时时间）
3. 添加API缓存机制
4. 优化Nginx配置
5. 优化前端请求策略

