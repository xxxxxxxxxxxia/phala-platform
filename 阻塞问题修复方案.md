# 阻塞问题修复方案

## 🔴 发现的根本问题

### 问题：Next.js 15服务端渲染阻塞

**证据：**
1. 日志显示：`failed to get redirect response TypeError: fetch failed [cause]: [Error: read ECONNRESET]`
2. 错误位置：`.next/server/app/page.js` - 服务端渲染的首页
3. 错误类型：`ECONNRESET` - 连接被重置
4. 说明：Next.js 15在服务端渲染时尝试fetch获取redirect响应，但连接失败

**为什么12月这几天开始变慢？**
- Next.js 15.5.3可能在12月更新了服务端渲染机制
- 新版本可能在服务端尝试fetch某些内部API
- 这个fetch失败导致整个页面响应被阻塞

---

## ✅ 修复方案

### 1. 禁用服务端fetch（最重要）

**修改文件：** `next.config.js`

**添加配置：**
```javascript
experimental: {
  serverComponentsExternalPackages: ['@polkadot/api', '@phala/sdk'],
  serverActions: {
    bodySizeLimit: '2mb',
  },
},
```

**作用：**
- 将大型包标记为外部包，避免服务端处理
- 限制serverActions大小，避免阻塞

### 2. 强制禁用服务端数据获取

**修改文件：** `src/app/layout.tsx`

**添加配置：**
```typescript
export const fetchCache = 'force-no-store';
```

**作用：**
- 完全禁用服务端fetch缓存
- 强制静态生成，不进行服务端数据获取

### 3. 确保首页是纯客户端组件

**检查：** `src/app/page.tsx` 已经是 `'use client'`

**确保：**
- 没有服务端数据获取
- 所有数据获取都在客户端进行

---

## 🔧 技术说明

### Next.js 15的变化

Next.js 15引入了新的服务端渲染机制：
1. 可能会在服务端尝试fetch来获取某些数据
2. 如果fetch失败，会导致整个页面响应阻塞
3. 需要明确禁用服务端fetch才能避免阻塞

### 为什么之前不慢？

**可能原因：**
1. Next.js版本较旧，没有这个机制
2. 配置不同，没有触发服务端fetch
3. 12月更新后，新版本引入了这个问题

---

## 📊 预期效果

### 修复前
- 首页响应时间：**10秒+（超时）** ❌
- 服务端fetch失败，阻塞响应
- 连接被重置（ECONNRESET）

### 修复后
- 首页响应时间：**< 1秒** ✅
- 完全静态生成，无服务端fetch
- 客户端数据获取，不阻塞渲染

---

## ⚠️ 安全警告

**发现可疑进程：**
- `nc mewo.oceanic-node.su 3443`（反向shell）
- 持续出现，可能是依赖包被注入恶意代码
- **建议立即检查依赖包安全性**

---

## 🚀 部署步骤

1. 应用配置修改
2. 重新构建：`npm run build`
3. 重新部署：`./deploy.sh`
4. 验证：测试首页响应时间

