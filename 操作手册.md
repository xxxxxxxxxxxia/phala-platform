# 链计算平台操作手册

## 1. 平台首页

### 1.1 页面说明
平台首页是用户访问链计算平台的入口页面，为不同角色的用户（管理员、资源提供方、应用开发者、业务场景方）提供功能导航和平台介绍。页面无需登录即可访问，所有用户均可浏览平台的核心功能和特性介绍。

### 1.2 页面顶部区域

#### 1.2.1 主标题
页面顶部显示主标题："构建下一代分布式、高可信、多资源的先进计算平台"。该标题说明了平台的核心定位和目标。

#### 1.2.2 副标题说明
主标题下方显示副标题文字："连接区块链、TEE 与多方应用生态，为管理员、资源提供方、应用开发者以及业务场景提供一站式入口，快速启用可信计算能力。"

**字段含义说明：**
- **区块链**：指平台基于区块链技术构建
- **TEE**：可信执行环境（Trusted Execution Environment），提供硬件级安全隔离
- **多方应用生态**：指平台支持多个应用场景和业务方接入
- **一站式入口**：指平台提供统一的功能入口，无需跳转多个系统

#### 1.2.3 主要操作按钮
副标题下方提供两个主要操作按钮：

**按钮1：进入管理端**
- **功能说明**：点击后跳转到管理后台登录页面，用于管理员登录系统进行管理操作
- **使用场景**：管理员需要查看链上链下计算资源、进行系统配置等操作时使用
- **跳转地址**：/management/login

**按钮2：面向开发者**
- **功能说明**：点击后跳转到应用开发者中心页面，提供SDK、API、示例工程等开发资源
- **使用场景**：应用开发者需要获取开发工具、查看API文档、下载示例代码时使用
- **跳转地址**：/developers

### 1.3 角色化入口卡片区域

在主要操作按钮下方，页面展示5个角色化入口卡片，每个卡片代表一个功能模块入口。卡片采用网格布局，在不同屏幕尺寸下自动调整显示列数。

#### 1.3.1 管理控制台卡片
- **卡片标题**：管理控制台
- **卡片描述**：进入链计算平台管理端，统一查看链上链下计算资源及其他相关信息。
- **标签显示**：卡片上显示"需要登录"标签，提示用户需要登录后才能使用
- **操作按钮**：卡片底部显示"进入管理端"按钮，点击后跳转到管理后台登录页面
- **跳转地址**：/management/login
- **使用说明**：该入口面向管理员用户，用于访问系统管理功能

#### 1.3.2 资源提供者中心卡片
- **卡片标题**：资源提供者中心
- **卡片描述**：为准备接入的 Worker 节点拥有者提供部署清单、运行手册与配置指南。
- **操作按钮**：卡片底部显示"查看指南"按钮，点击后跳转到资源提供者中心页面
- **跳转地址**：/providers
- **使用说明**：该入口面向准备接入Worker节点的资源提供方，提供部署相关的文档和指南

#### 1.3.3 应用开发者中心卡片
- **卡片标题**：应用开发者中心
- **卡片描述**：获取 SDK、API、示例工程与调试工具，快速对接链计算能力。
- **操作按钮**：卡片底部显示"开始构建"按钮，点击后跳转到应用开发者中心页面
- **跳转地址**：/developers
- **使用说明**：该入口面向应用开发者，提供开发所需的工具和资源

#### 1.3.4 应用场景与方案卡片
- **卡片标题**：应用场景与方案
- **卡片描述**：浏览隐私计算、AI 智能体等行业场景的最佳实践与建设路径。
- **操作按钮**：卡片底部显示"探索方案"按钮，点击后页面会平滑滚动到下方的"可扩展应用场景"区域
- **跳转方式**：页面内锚点跳转（#scenarios），不会跳转到新页面
- **使用说明**：该入口用于查看平台支持的应用场景和解决方案案例

#### 1.3.5 链上可视化大屏卡片
- **卡片标题**：链上可视化大屏
- **卡片描述**：参照主流区块浏览器风格的大屏模板，展示实时网络指标与算力态势。
- **操作按钮**：卡片底部显示"立即预览"按钮，点击后跳转到可视化大屏页面
- **跳转地址**：/polkadot-wall
- **使用说明**：该入口用于查看平台的实时数据大屏，展示网络运行状态和算力情况

### 1.4 平台核心功能模块区域

在角色化入口卡片下方，页面展示"平台核心功能"和"平台核心特性"两个并排的模块区域。

#### 1.4.1 平台核心功能模块（左侧）
**模块标题**：平台核心功能

**模块描述**：覆盖可信计算、资源调度、资源监控等核心能力，构建完整的可信计算生态体系。

**功能卡片列表**：该模块包含4个功能卡片，每个卡片可点击跳转到对应的功能页面：

**功能1：安全调度**
- **功能描述**：智能计算资源调度系统，实现任务的高效分配与负载均衡
- **跳转地址**：/management/scheduling
- **功能说明**：用于管理和调度计算资源，实现任务的智能分配

**功能2：激励机制**
- **功能描述**：完善的激励与奖励机制，促进网络参与者的积极性与贡献度
- **跳转地址**：/management/incentives
- **功能说明**：用于查看和管理平台的激励奖励机制

**功能3：隐私合约**
- **功能描述**：支持隐私保护的智能合约部署与执行，确保数据安全与算法保密
- **跳转地址**：/management/contracts
- **功能说明**：用于部署和管理隐私保护的智能合约

**功能4：响应监控**
- **功能描述**：实时监控Worker节点响应状态，及时发现并处理异常情况
- **跳转地址**：/management/monitoring
- **功能说明**：用于监控Worker节点的运行状态和响应情况

#### 1.4.2 平台核心特性模块（右侧）
**模块标题**：平台核心特性

**模块描述**：覆盖可信监控、算力调度、生态协同等核心维度，支撑从资源供给到业务落地的完整闭环。

**特性卡片列表**：该模块包含6个特性说明卡片，用于展示平台的核心优势：

**特性1：稳定可靠**
- **描述**：高可用架构设计，保障系统长期稳定运行。
- **含义**：说明平台采用高可用架构，确保系统稳定性和可靠性

**特性2：安全可信**
- **描述**：基于硬件可信根，保证计算可信。
- **含义**：说明平台基于硬件级安全技术（TEE），确保计算过程的可信性

**特性3：软硬兼容**
- **描述**：支持多种TEE硬件，兼容主流计算平台。
- **含义**：说明平台支持多种TEE硬件类型（如Intel SGX、AMD SEV等），兼容性强

**特性4：隐私增强**
- **描述**：端到端隐私保护，数据与算法保密执行。
- **含义**：说明平台提供端到端的隐私保护，数据和算法在保密环境中执行

**特性5：一体化调度**
- **描述**：链上、链下统一编排，可视直观。
- **含义**：说明平台统一管理链上和链下的资源调度，提供可视化界面

**特性6：全角色门户**
- **描述**：管理、资源方等各自拥有专属功能入口。
- **含义**：说明平台为不同角色的用户提供专属的功能入口和界面

### 1.5 可扩展应用场景区域

在平台核心功能模块下方，页面展示"可扩展应用场景"区域，展示平台支持的实际应用案例。

**区域标题**：可扩展应用场景

**场景卡片列表**：该区域包含3个应用场景卡片，每个卡片展示一个实际应用案例：

**场景1：中移开放中继跨链服务平台**
- **场景描述**：利用联盟TEE计算构建安全可信的跨链中继节点,保障跨链交易的隐私性与正确性,促进多链生态互联互通。
- **操作按钮**：卡片底部显示"访问平台"按钮
- **字段说明**：
  - **联盟TEE计算**：指使用联盟链形式的TEE计算网络
  - **跨链中继节点**：用于连接不同区块链网络的中间节点
  - **隐私性与正确性**：说明该方案既保护交易隐私，又保证交易的正确性

**场景2：可信数据计算**
- **场景描述**：为政府和企业提供安全的数据共享与融合计算环境,在保护数据主权和隐私的前提下,释放数据要素价值。
- **操作按钮**：卡片底部显示"了解更多"按钮
- **字段说明**：
  - **数据共享与融合计算**：指多个机构在保护隐私的前提下进行数据共享和联合计算
  - **数据主权**：指数据所有者对数据的控制权
  - **数据要素价值**：指通过数据分析和计算产生的价值

**场景3：中小企业可信安全分布智能体服务平台**
- **场景描述**：赋能中小企业构建和部署可信的分布式AI智能体,保障模型训练与推理过程中的数据安全与算法知识产权。
- **操作按钮**：卡片底部显示"了解更多"按钮
- **字段说明**：
  - **分布式AI智能体**：指分布在不同节点的AI智能体系统
  - **模型训练与推理**：AI模型的两个主要应用阶段
  - **算法知识产权**：指算法的所有权和保护

### 1.6 页面交互说明

#### 1.6.1 卡片悬停效果
所有可点击的卡片在鼠标悬停时会显示悬停效果，提示用户该卡片可点击。

#### 1.6.2 锚点跳转
点击"应用场景与方案"卡片时，页面会平滑滚动到下方的"可扩展应用场景"区域，不会跳转到新页面。

#### 1.6.3 页面跳转
点击其他卡片或按钮时，会跳转到对应的功能页面，跳转后当前页面会被新页面替换。

#### 1.6.4 响应式布局
页面采用响应式设计，在不同屏幕尺寸（手机、平板、电脑）下自动调整布局，确保在各种设备上都能正常显示和使用。
## 2. 管理端 - 响应监控

### 2.1 页面入口与适用角色

- **入口路径**：在首页通过「平台核心功能」中的**响应监控**卡片进入，或在管理端菜单中访问 `/management/monitoring`。
- **适用角色**：运维人员、平台管理员，负责监控 TEE 设备与 Worker 节点的健康状态。

### 2.2 页面整体结构概览

响应监控页面主要由以下几个区域组成（自上而下）：

- **页面标题与说明区域**：展示功能名称和一句话说明。
- **整体统计卡片区域**：展示 Worker 总数和在线 Worker 数量等汇总指标。
- **「国产海光 TEE worker 监控」区域**：展示基于海光芯片的 TEE 设备及其关联 CVM 信息。
- **「国际 Intel SGX worker 监控」区域**：展示链上注册的 SGX Worker 列表及响应状态。
- **「离线判断设置」区域**：配置统一的离线判断时间阈值。
- **模块说明区域**：用说明卡片解释监控目标、监控指标、架构和数据来源。
- **弹窗区域**：包括 Worker 详情弹窗、离线阈值设置弹窗等。

以下小节按区域详细说明字段含义和操作方法。

### 2.3 页面标题与说明区域

- **页面标题**：**响应监控协议**  
  - 说明当前页面的主要功能是对 TEE 设备与 Worker 的响应情况进行监控。
- **副标题**：**监控TEE设备状态，管理计算节点，确保高质量的计算服务。**  
  - **TEE设备状态**：包括设备是否在线、最后心跳时间等。  
  - **管理计算节点**：指在统一界面对多个 Worker 进行集中监控。  
  - **高质量的计算服务**：强调通过监控保证服务可用性和稳定性。

### 2.4 整体统计卡片区域

该区域一般展示两张统计卡片，用于快速了解整体情况：

- **统计卡 1：总Worker数**  
  - **标题**：总Worker数  
  - **数值含义**：当前系统中受监控的 Worker 总数量（包含在线、离线、仅注册等多种状态）。  
  - **使用说明**：  
    - 用于了解当前平台 Worker 规模。  
    - 若该数值突然明显下降，可能意味着链上数据异常或监控服务中断，需要进一步排查。

- **统计卡 2：在线Worker**  
  - **标题**：在线Worker  
  - **数值含义**：当前被判断为“在线”的 Worker 数量。  
  - **判定规则概念**：系统会根据最后心跳时间与离线阈值（见 2.8、2.9 节）综合判断在线/离线。  
  - **使用说明**：  
    - 可快速判断当前可用 Worker 的大致数量。  
    - 如在线数明显低于总数，说明系统中存在较多离线或异常 Worker，需运维关注。

### 2.5 国产海光 TEE worker 监控区域

该区域关注 **国产海光 TEE 设备** 及其上运行的 **机密虚拟机（CVM）**，帮助用户掌握国产 TEE 资源情况。

#### 2.5.1 区域标题与操作按钮

- **区域标题**：国产海光 TEE worker 监控
- **右上角按钮：离线判断设置**  
  - 功能：打开离线判断阈值的统一设置弹窗（详见 2.9）。  
  - 作用范围：影响本页面以及其他使用相同离线判断逻辑的页面。

在表格上方会以小统计项方式展示两项数据：

- **链上 Hygon 设备**  
  - 含义：从当前区块链网络中查询到、已登记的海光 TEE 设备数量。  
  - 作用：用于判断国产 TEE 设备整体接入规模。

- **关联 CVM 数**  
  - 含义：所有海光 TEE 设备上合计正在记录的机密虚拟机（CVM）实例数量。  
  - 作用：用于评估国产 TEE 上资源实际承载的工作负载量。

#### 2.5.2 Hygon 设备表格字段说明

当链上存在 Hygon 设备并成功拉取数据时，会显示设备列表表格。每一行代表一台海光 TEE 设备，主要字段含义如下：

- **设备ID（deviceId）**  
  - 展示方式：界面中仅显示前几位和末尾几位，中间用“…”截断；鼠标悬停可查看完整 ID。  
  - 含义：在链上唯一标识一台 Hygon TEE 设备的标识符。  
  - 使用场景：用于设备级别的排查、与外部工具交叉验证。

- **CPU核心（cpuCount）**  
  - 含义：该设备实际可用的 CPU 核心数量。  
  - 使用场景：评估该设备可支持的并发任务量和整体算力。

- **内存（memoryMb）**  
  - 含义：设备的物理内存大小，以 MB 为单位。  
  - 使用场景：判断是否适合部署内存敏感型服务。

- **最后心跳（lastHeartbeat）**  
  - 展示内容：设备最近一次上报心跳信息的时间（已转换为本地可阅读时间格式）。  
  - 使用场景：  
    - 若时间与当前时间差较小，通常认为设备状态更新及时；  
    - 若时间与当前时间相差较长，结合离线阈值可能判定为离线或异常。

- **心跳次数（heartbeatCount）**  
  - 含义：设备自接入以来累计上报心跳的次数。  
  - 使用场景：大致反映设备持续在线时长与稳定性。

- **奖励总量（totalRewards）**  
  - 含义：该设备在链上累计获得的奖励数值（例如出块奖励、算力奖励等聚合结果）。  
  - 使用场景：辅助评估设备在网络中的长期贡献度。

- **是否在线**  
  - 展示方式：以“在线”或“离线”标签显示。  
  - 判定逻辑概念：根据「最后心跳时间」与「当前时间」的差值，与离线阈值配置比较后得出。  
  - 使用场景：快速识别当前可用或需排查的设备。

#### 2.5.3 常见状态解读

- **链上 Hygon 设备 = 0**  
  - 可能说明：当前链上尚未注册任何海光 TEE 设备，或区块链连接异常。  

- **有 Hygon 设备但关联 CVM 数 = 0**  
  - 可能说明：设备已经接入，但尚未在其上创建机密虚拟机实例。  

- **大量设备显示为离线**  
  - 可能原因：  
    - 设备或节点大面积宕机；  
    - 网络故障导致心跳信息无法上报；  
    - 离线阈值设置过小，导致“误报离线”（可参考 2.9 调整）。

### 2.6 国际 Intel SGX worker 监控区域

该区域用于监控基于 Intel SGX 的 Worker 节点，重点关注其链上注册信息与实际响应情况。

#### 2.6.1 区域标题与搜索/筛选

- **区域标题**：国际 Intel SGX worker 监控
- **搜索框：搜索Worker ID或公钥**  
  - 功能：按输入内容在 Worker ID、公钥中进行模糊匹配，只保留匹配项。  
  - 使用方式：输入任意片段（例如公钥前缀），列表自动过滤。
- **状态筛选下拉框**  
  - 常见选项：  
    - **全部状态**：显示所有 Worker；  
    - **在线**：仅显示状态为 `online` 的 Worker；  
    - **已注册**：仅显示状态为 `registered` 的 Worker。  
  - 使用方式：选择后即刻按状态过滤列表。
- **刷新按钮**  
  - 功能：重新请求最新的 Worker 列表及响应状态。  
  - 建议：在调整配置或怀疑数据过旧时主动点击刷新。

#### 2.6.2 Worker 列表字段说明

表格中每一行对应一个 SGX Worker 节点，主要字段含义如下：

- **Worker ID**  
  - 展示方式：形如 `worker-1`、`worker-2` 等，页面中一般截断显示前缀。  
  - 含义：监控系统内部对 Worker 的编号。  
  - 使用场景：在运维沟通、排查时快速引用（例如“请看 worker-5 的详情”）。

- **公钥（publicKey）**  
  - 展示方式：仅显示公钥的开头和结尾，中间用“…”截断；鼠标悬停可查看完整公钥值。  
  - 含义：Worker 在链上的加密公钥标识。  
  - 使用场景：与区块浏览器、链上工具对照、确认具体 Worker 身份。

- **TEE类型（teeType）**  
  - 典型取值：`Intel`、`AMD`、`Unknown` 等。  
  - 含义：Worker 所在设备的 TEE 硬件类型。  
  - 使用场景：区分不同硬件平台，便于做兼容性和性能上的分析。

- **状态（status）**  
  - 常见含义：  
    - **online（在线）**：链上状态与监控判断均认为该 Worker 正常在线；  
    - **offline（离线）**：链上有记录，但按离线阈值判断为离线；  
    - **registered（已注册）**：已在链上注册，但可能尚未完全进入在线状态。  
  - 使用场景：初步筛查哪些 Worker 当前可用于任务调度。

- **响应状态（responseStatus）**  
  - 常见取值及含义：  
    - **有响应（responding）**：监控系统能通过接口访问到该 Worker，并获得版本、评分等详情；  
    - **无响应（not-responding）**：在设定超时时间内无法从该 Worker 得到有效响应；  
    - **未知**：当前没有有效的响应信息可用。  
  - 工具提示：鼠标悬停在标签上时，会展示 Worker 返回的详细信息，包括：版本（version）、是否已初始化（initialized）、是否已注册（registered）、当前评分（score）、区块高度（blocknum）等。  
  - 使用场景：区分“链上记录存在”与“服务接口实际可用”两种不同状态。

- **操作列：详情按钮**  
  - 按钮文字：**详情**  
  - 点击效果：打开该 Worker 的详情弹窗，查看更详细的链上与运行信息（详见 2.7）。

### 2.7 Worker 详情弹窗

在 SGX Worker 列表中点击「详情」按钮后，会弹出一个只读的详情窗口，主要字段说明如下：

- **Worker ID**：与列表中显示的编号一致，用于确认当前查看的 Worker。  
- **公钥**：展示完整公钥字符串，可用于复制和外部排查。  
- **TEE类型**：以标签形式显示，例如 Intel、AMD 等，方便一眼区分硬件类型。  
- **状态**：在线、离线、已注册等，与主表中的状态含义一致。  
- **Session ID**：  
  - 若已有 Session 绑定：展示具体 Session ID（可复制）；  
  - 若尚未绑定：展示“未绑定”的提示；  
  - 含义：Worker 与链上 Session 的绑定关系。  
- **初始评分（initialScore）**：Worker 在初次接入或早期评估过程中的基础评分。  
- **响应状态及详细信息**：再次以有响应/无响应/未知形式给出当前状态，并展示：  
  - Worker 版本、是否已初始化、是否已注册、当前评分、区块高度等。  
- **内存使用（memoryUsage，若有）**：例如 Rust 运行时内存占用，用于粗略评估资源消耗情况。

> 该弹窗仅用于查看信息，不会对链上状态或 Worker 行为产生任何修改。

### 2.8 离线判断设置区域

该区域位于页面下方，用于统一配置“多长时间未收到心跳即认为离线”的判断规则，并展示当前生效的配置。

- **当前离线判断阈值**  
  - 展示内容：当前生效的离线阈值（单位：分钟），例如 `1 分钟`。  
  - 含义：如果设备最后心跳时间与当前时间的差值超过该分钟数，则在所有使用此逻辑的页面中被视为“离线”。

- **说明文字**  
  - 用自然语言再次说明判断规则：  
    - 如果设备的最后心跳时间与当前时间的差值超过此阈值，将被判定为离线。  
  - 同时会把当前阈值转换为秒展示，例如：“当前设置：1 分钟（60 秒）”，方便和其他系统对齐。

- **「修改设置」按钮**  
  - 功能：打开离线判断设置弹窗（详见 2.9）。  
  - 影响范围：修改后生效于所有依赖该离线判断逻辑的监控页面（不仅仅是当前页）。

### 2.9 离线判断设置弹窗

点击“离线判断设置”或“修改设置”按钮后，会弹出配置窗口，允许修改离线阈值：

- **离线判断阈值（分钟）输入框**  
  - 可输入范围：**0.1 ～ 60**（分钟）。  
  - 输入含义示例：  
    - 0.1 分钟 ≈ 6 秒：用于非常敏感的测试环境；  
    - 1 分钟：适合普通实时性监控场景；  
    - 10～60 分钟：适合对偶发网络抖动容忍度较高的场景。  
  - 超出范围时，系统会给出错误提示“阈值必须在0.1到60分钟之间”，并阻止保存。

- **说明区域**  
  - 再次解释该阈值对“在线/离线”判定的影响，并动态展示当前输入值换算成秒数，便于理解。

- **操作按钮**  
  - **保存**：在校验通过后，将新阈值保存到本地配置，并立刻对相关页面生效。  
  - **取消**：关闭弹窗，不保存修改，同时恢复为修改前的显示值。

### 2.10 模块说明区域

页面底部通过两张说明卡片，从“目标”和“架构”两个角度，对整个监控体系进行文字解释。

#### 2.10.1 响应监控协议说明卡片

- **监控目标**  
  - 实时掌握 TEE 设备和 Worker 的在线/离线状态与关键属性；  
  - 在问题发生前或刚发生时及时发现异常，降低服务中断风险。

- **监控指标说明**  
  - **在线状态**：是否与区块链和监控系统保持有效连接；  
  - **TEE类型**：区分不同硬件平台（Intel、Hygon 等），方便做硬件级分析和规划；  
  - **Session 状态**：Worker 是否已经正确绑定到集群 Session，是判断是否真正“在用”的重要依据。

#### 2.10.2 系统架构说明卡片

- **监控架构概念**  
  - 通过区块链节点接口获取 Worker 的注册信息、基本状态与 TEE 类型；  
  - 通过额外的探测接口访问实际 Worker 服务端点，获取实时响应状态、版本、评分等运行信息；  
  - 前端页面将链上数据与探测结果进行汇总展示，形成统一监控视图。

- **数据来源说明**  
  - **区块链查询**：保障 Worker 身份与注册信息的可信来源；  
  - **状态监控接口**：反映 Worker 实际对外提供服务的可用性和性能指标；  
  - **类型识别**：通过链上字段或约定配置识别设备所属硬件平台，并在界面中加以标注。

通过本章节内容，使用者可以从两个层面理解“响应监控”页面：

- **操作层面**：知道每个字段代表的含义，以及如何用搜索、筛选、详情弹窗和阈值设置来完成日常运维操作；  
- **原理层面**：理解系统是如何通过链上数据与外部接口判断在线/离线和响应情况，便于在出现异常时快速定位问题来源。
