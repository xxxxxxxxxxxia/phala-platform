# 多用户访问性能问题修复

## 🔍 问题分析

### 测试结果
- **第一个用户访问**：115-130秒 ❌
- **后续用户访问**：0.05秒 ✅

### 根本原因

1. **页面编译机制**：
   - Next.js在standalone模式下，第一次访问页面时需要编译
   - 编译后的页面会缓存在内存中（`onDemandEntries`）
   - 后续用户访问时直接使用缓存的页面，所以很快

2. **为什么第一个用户慢**：
   - 服务器启动时，页面还没有被编译
   - 第一个用户访问时触发编译，需要115-130秒
   - 编译完成后，页面缓存在内存中

3. **为什么后续用户快**：
   - 页面已经在内存中，直接使用缓存
   - 不需要重新编译

## ✅ 修复方案

### 1. 增加页面缓存时间

**修改文件**：`next.config.js` 和 `server.js`

**配置**：
```javascript
onDemandEntries: {
  maxInactiveAge: 60 * 1000 * 30,  // 30分钟（从10分钟增加）
  pagesBufferLength: 50,            // 50个页面（从25增加）
}
```

**作用**：
- 页面在内存中保留30分钟
- 可以缓存50个页面
- 减少页面被清理的频率

### 2. 服务器预热

**新增文件**：`warmup-server.js`

**功能**：
- 服务器启动后自动访问首页
- 触发页面编译，避免第一个用户等待

**修改文件**：`server.js`

**添加代码**：
```javascript
// 服务器启动后预热首页
setTimeout(() => {
  const warmup = require('./warmup-server.js');
  warmup(currentPort, hostname === '0.0.0.0' ? 'localhost' : hostname)
    .then(() => {
      console.log('🎉 首页预热完成，新用户访问将更快');
    })
    .catch((err) => {
      console.warn('⚠️  首页预热失败（不影响服务）:', err.message);
    });
}, 3000); // 等待3秒后开始预热
```

## 📊 预期效果

### 修复前
- 第一个用户：115-130秒 ❌
- 后续用户：0.05秒 ✅

### 修复后
- 第一个用户：0.05秒 ✅（通过预热）
- 后续用户：0.05秒 ✅

## ⚠️ 注意事项

1. **预热可能失败**：如果服务器启动慢，预热可能超时，但不影响服务
2. **内存使用**：增加缓存时间和页面数量会增加内存使用
3. **首次编译不可避免**：即使预热，如果页面被清理，第一个访问者仍需要等待编译

## 🔍 验证方法

```bash
# 重启服务器
docker restart phala-platform-frontend

# 等待15秒后测试
sleep 15
curl -w "\n时间: %{time_total}s\n" -o /dev/null -s http://localhost:3000/

# 应该 < 1秒（如果预热成功）
```

## 🚀 进一步优化建议

如果问题仍然存在，可以考虑：

1. **预渲染所有页面**：在构建时预渲染，但会增加构建时间
2. **使用CDN**：将静态页面放在CDN上
3. **增加服务器资源**：确保有足够的内存和CPU

