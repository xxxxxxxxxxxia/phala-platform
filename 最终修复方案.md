# 最终修复方案 - Next.js 15服务端渲染阻塞问题

## 🔴 根本原因确认

### 问题：Next.js 15服务端fetch阻塞

**证据：**
1. 日志：`failed to get redirect response TypeError: fetch failed [cause]: [Error: read ECONNRESET]`
2. 错误位置：`.next/server/app/page.js` - 服务端渲染的首页
3. 错误类型：`ECONNRESET` - 连接被重置
4. 说明：Next.js 15在服务端尝试fetch获取redirect响应时失败，阻塞整个页面

**为什么12月这几天开始变慢？**
- Next.js 15.5.3可能在12月更新了服务端渲染机制
- 新版本在服务端尝试fetch某些内部API（如redirect响应）
- 这个fetch失败导致整个页面响应被阻塞10秒+

---

## ✅ 已实施的修复

### 1. 修复redirect页面（最重要）✅

**文件：** `src/app/login/page.tsx` 和 `src/app/register/page.tsx`

**修改：**
- 从服务端`redirect()`改为客户端`router.replace()`
- 避免Next.js在服务端尝试fetch redirect响应

**作用：**
- 完全避免服务端fetch
- redirect在客户端进行，不阻塞服务端渲染

### 2. 禁用服务端fetch ✅

**文件：** `next.config.js`

**添加：**
```javascript
experimental: {
  serverComponentsExternalPackages: ['@polkadot/api', '@phala/sdk'],
  serverActions: {
    bodySizeLimit: '2mb',
  },
}
```

**作用：**
- 将大型包标记为外部包，避免服务端处理
- 限制serverActions大小

### 3. 强制禁用服务端数据获取 ✅

**文件：** `src/app/layout.tsx`

**添加：**
```typescript
export const fetchCache = 'force-no-store';
```

**作用：**
- 完全禁用服务端fetch缓存
- 强制静态生成

### 4. 修复预热脚本IPv6问题 ✅

**文件：** `warmup-server.js` 和 `server.js`

**修改：**
- 使用`127.0.0.1`而不是`localhost`
- 强制使用IPv4（`family: 4`）
- 减少超时时间到5秒

**作用：**
- 避免IPv6连接失败
- 快速失败，不阻塞

---

## 📊 预期效果

### 修复前
- 首页响应时间：**10秒+（超时）** ❌
- 服务端fetch失败，阻塞响应
- 连接被重置（ECONNRESET）

### 修复后
- 首页响应时间：**< 1秒** ✅
- 完全静态生成，无服务端fetch
- redirect在客户端进行

---

## 🚀 部署步骤

1. **重新构建：**
   ```bash
   cd /root/tmp/phala-platform
   npm run build
   ```

2. **重新部署：**
   ```bash
   ./deploy.sh
   ```

3. **验证：**
   ```bash
   curl -w "\n时间: %{time_total}s\n" -o /dev/null -s http://localhost:3000
   ```
   预期：时间 < 1秒

---

## ⚠️ 重要说明

### 这不是页面大小问题！

**真正的问题：**
1. Next.js 15在服务端尝试fetch redirect响应
2. 这个fetch失败（ECONNRESET）
3. 导致整个页面响应被阻塞10秒+

**修复后：**
- redirect在客户端进行
- 服务端完全静态生成
- 无任何服务端fetch

---

## 🔍 验证方法

部署后检查：
1. 首页响应时间 < 1秒
2. 日志中无`failed to get redirect response`错误
3. 无ECONNRESET错误

